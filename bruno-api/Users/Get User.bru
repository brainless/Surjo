meta {
  name: Get User
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users/{{userId}}
  body: none
  auth: none
}

vars {
  userId: user-id-here
}

tests {
  test("should return 200 or 404", function() {
    expect([200, 404]).to.include(res.getStatus());
  });
}